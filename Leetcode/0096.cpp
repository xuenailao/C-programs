#include<vector>

using namespace std;

class Solution {
public:
	int maxweightpackage(vector<int> & weight, int cap) {
		int num = weight.size();

		vector<vector<int>> dp(num, vector<int>(cap));


	}
}